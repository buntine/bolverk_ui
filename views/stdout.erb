    <div id="stdout">
      <h2>Standard output</h2>
      <% if session[:stdout].empty? %>
      <p>-- EOF --</P>
      <% else %>
      <p><%= session[:stdout] %></P>
      <p><a href="/flush" id="flush_stdout">Flush stdout</a></p>
      <% end %>
    </div>
